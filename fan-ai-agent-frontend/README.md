# Fan AI Agent 前端

Vue 3 + Vite 单页应用，包含首页、恋爱大师和超级智能体。完整启动和部署说明见根目录 README。

```shell
npm ci
npm run dev
npm run build
```

开发端口 3000，后端地址 `http://localhost:8123/api`。生产构建位于 `dist`，使用同域 `/api`，需配置 Nginx 指向自己的后端。

用户协议、隐私政策和商务合作仍为占位链接，上线前请补充真实内容。
